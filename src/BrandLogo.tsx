import OfficialTempusMark from "./imports/Group9";

/**
 * Assinatura horizontal oficial Tempus, confirmada no Manual de Marca.
 * Mantém uma única fonte de verdade para marca, cabeçalhos e rodapé.
 */
export default function BrandLogo() {
  return <OfficialTempusMark />;
}
